name: fmvvm
description: A feature folder structure for Flutter (controllers, models, repositories, screens, widgets) with MVVM architecture and Provider state management.
version: 0.1.0+1
vars:
  feature_name:
    type: string
    description: The name of the feature (e.g. auth, profile, chat)
    default: feature
